<%= form_with(model: accessory, local: true) do |form| %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <br><br>
  <div class="actions" style="float:right">
    <%= form.submit %>
  </div>
<% end %>
